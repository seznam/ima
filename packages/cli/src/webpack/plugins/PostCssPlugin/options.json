{
  "title": "PostCssPlugin",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "plugins": {
      "type": "array",
      "description": "Array of postcss plugins to process."
    },
    "filter": {
      "instanceof": "Function",
      "description": "Optional assets filter function, by default all css assets are processed."
    }
  }
}

{
  "name": "@ima/devtools-scripts",
  "version": "17.12.2",
  "description": "IMA.js devtool script used in the @ima/devtools.",
  "keywords": [
    "IMA",
    "IMA.js",
    "Chrome",
    "Devtools",
    "Inject Scripts"
  ],
  "bugs": {
    "url": "https://github.com/seznam/ima/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/seznam/ima.git",
    "directory": "packages/devtools-scripts"
  },
  "license": "MIT",
  "author": "Miroslav Jancarik <miroslav.jancarik@firma.seznam.cz>",
  "contributors": [
    {
      "name": "Jan Šimeček",
      "email": "jan.simecek@firma.seznam.cz"
    }
  ],
  "main": "./dist/main.string.js",
  "scripts": {
    "build": "npm run build:script && npm run build:string",
    "lint": "eslint './**/*.{js,jsx,ts,tsx}'",
    "build:script": "rollup --config",
    "build:string": "babel --plugins @babel/plugin-transform-template-literals ./dist/main.mjs | (echo '// DO NOT MODIFY FILE, FILE IS AUTO GENERATED FROM OTHER REPOSITORIES \nexport default function(hooks) { return `' && cat && echo 'createDevtool(({ importIMAClass, aop, createHook, hookName, createCallTrap, emit }) => { ${Object.values(hooks).filter(hook => hook.enabled).map(hook => hook.code).join(\"\\\\n\\\\n\")} });' && echo '`; }') > ./dist/main.string.js",
    "dev": "rollup --config --watch",
    "test": "echo \"Error: no test specified\" && exit 0",
    "preversion": "npm test"
  },
  "dependencies": {
    "easy-uid": "^2.0.2",
    "to-aop": "^0.5.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.7",
    "@babel/plugin-transform-template-literals": "^7.16.7"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
